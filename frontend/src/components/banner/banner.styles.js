import styled from "styled-components";

export const MainWrapper = styled.div`
  .sale-banner {
    overflow: hidden;
    display: block;
    max-width: 100%;
    margin: 0;
    text-align: center;
    line-height: 0;
  }

  .sale-banner img {
    max-width: 100%;
  }
`;
